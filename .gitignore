# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Note: _tuner.html and _fonts.html are intentionally committed.
# GitHub Pages runs Jekyll, which skips anything starting with "_", so they
# stay in the repo for future edits but never appear on the live site.
# If you ever add a .nojekyll file, they WILL become publicly reachable.
